
export const GRID_SIZE = 20;
export const INITIAL_SPEED = 150;
export const MIN_SPEED = 60;
export const SPEED_DECREMENT = 2;

// Slider Configuration (ms delay)
export const SLIDER_MIN_DELAY = 50;  // Fastest
export const SLIDER_MAX_DELAY = 300; // Slowest

// Defines the board dimensions in grid units (not pixels)
export const BOARD_WIDTH = 25;
export const BOARD_HEIGHT = 25;

export const KEYS = {
  ARROW_UP: 'ArrowUp',
  ARROW_DOWN: 'ArrowDown',
  ARROW_LEFT: 'ArrowLeft',
  ARROW_RIGHT: 'ArrowRight',
  W: 'w',
  S: 's',
  A: 'a',
  D: 'd',
  SPACE: ' ',
};
